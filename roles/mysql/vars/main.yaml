# MYSQL_ROOT_PASSWORD: roboshop123

# for terraform-roboshop-module
MYSQL_ROOT_PASSWORD: "{{ lookup('amazon.aws.aws_ssm', '/roboshop/{{ env }}/mysql/mysql_root_password', region='us-east-1', decrypt=True ) }}"